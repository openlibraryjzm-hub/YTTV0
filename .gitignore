# Dependencies
node_modules/
# Note: package-lock.json is typically committed for consistency

# Build outputs
dist/
*.tsbuildinfo

# Rust build artifacts
src-tauri/target/
src-tauri/gen/
# Note: Cargo.lock is typically committed for applications

# Binary files and archives
src-tauri/bin/
*.7z
*.zip
# Note: DLLs in src-tauri/lib/ are kept (needed for build)
# DLLs in target/ are ignored via src-tauri/target/ above
*.exe
*.so
*.dylib

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# IDE and editor files
.idea/
*.swp
*.swo
*~
# Keep .vscode/extensions.json but ignore user-specific settings
.vscode/settings.json
.vscode/launch.json
.vscode/tasks.json
*.code-workspace

# Environment variables
.env
.env.local
.env.*.local
.env.development
.env.production

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Temporary files
*.tmp
*.temp
*.cache
.cache/

# Debug files
debug_permissions.cjs
debug_permissions.js

# Nested/duplicate directories (likely build artifacts)
src-tauri/src-tauri/

# Coverage reports
coverage/
*.lcov
.nyc_output/

# Optional: Keep database files (as per user request)
# playlists.db - NOT ignored
